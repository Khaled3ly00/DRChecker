<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="90vh" viewBox="0 0 1200 800" preserveAspectRatio="xMidYMid meet">
  <style>
    .violation-label {
      visibility: hidden;
      pointer-events: none;
    }
    .violation:hover .violation-label {
      visibility: visible;
    }
    .violation {
      cursor: pointer;
    }
    .legend-item.disabled .legend-label {
      fill: darkgray;
    }
    .legend-item.disabled rect {
      opacity: 0.35;
    }
  </style>
  <g id="layout">
  <g class="shape layer-M1">
    <title>Shape ID: 1&#10;Layer: M1</title>
  <polygon points="40,637.5 135,637.5 135,162.5 40,162.5 " fill="cyan" fill-opacity="0.35" stroke="cyan" stroke-width="2" />
  </g>
  <g class="shape layer-M1">
    <title>Shape ID: 2&#10;Layer: M1</title>
  <polygon points="230,637.5 515,637.5 515,400 230,400 " fill="cyan" fill-opacity="0.35" stroke="cyan" stroke-width="2" />
  </g>
  <g class="shape layer-VIA1">
    <title>Shape ID: 3&#10;Layer: VIA1</title>
  <polygon points="277.5,542.5 325,542.5 325,495 277.5,495 " fill="yellow" fill-opacity="0.35" stroke="yellow" stroke-width="2" />
  </g>
  <g class="shape layer-M1">
    <title>Shape ID: 4&#10;Layer: M1</title>
  <polygon points="610,637.5 990,637.5 990,400 610,400 " fill="cyan" fill-opacity="0.35" stroke="cyan" stroke-width="2" />
  </g>
  </g>
  <g id="violations">
    <g class="violation" data-layers="layer-M1">
    <line class="violation-edge" x1="135" y1="637.5" x2="135" y2="162.5" stroke="red" stroke-width="3" stroke-linecap="round" />
    <line class="violation-edge" x1="40" y1="162.5" x2="40" y2="637.5" stroke="red" stroke-width="3" stroke-linecap="round" />
  <g>
    <line class="violation-marker" x1="135" y1="400" x2="40" y2="400" stroke="red" stroke-width="2" stroke-linecap="round" />
    <circle class="violation-point" cx="135" cy="400" r="4" fill="red" />
    <circle class="violation-point" cx="40" cy="400" r="4" fill="red" />
    <text class="violation-label" x="87.5" y="393" font-size="14" text-anchor="middle" fill="red">MinWidth 2/3</text>
  </g>
    </g>
    <g class="violation" data-layers="layer-M1">
    <line class="violation-edge" x1="135" y1="637.5" x2="135" y2="162.5" stroke="red" stroke-width="3" stroke-linecap="round" />
    <line class="violation-edge" x1="230" y1="400" x2="230" y2="637.5" stroke="red" stroke-width="3" stroke-linecap="round" />
  <g>
    <line class="violation-marker" x1="135" y1="400" x2="230" y2="400" stroke="red" stroke-width="2" stroke-linecap="round" />
    <circle class="violation-point" cx="135" cy="400" r="4" fill="red" />
    <circle class="violation-point" cx="230" cy="400" r="4" fill="red" />
    <text class="violation-label" x="182.5" y="393" font-size="14" text-anchor="middle" fill="red">MinSpacing 2/3</text>
  </g>
    </g>
    <g class="violation" data-layers="layer-M1">
    <line class="violation-edge" x1="515" y1="637.5" x2="515" y2="400" stroke="red" stroke-width="3" stroke-linecap="round" />
    <line class="violation-edge" x1="610" y1="400" x2="610" y2="637.5" stroke="red" stroke-width="3" stroke-linecap="round" />
  <g>
    <line class="violation-marker" x1="515" y1="400" x2="610" y2="400" stroke="red" stroke-width="2" stroke-linecap="round" />
    <circle class="violation-point" cx="515" cy="400" r="4" fill="red" />
    <circle class="violation-point" cx="610" cy="400" r="4" fill="red" />
    <text class="violation-label" x="562.5" y="393" font-size="14" text-anchor="middle" fill="red">MinSpacing 2/3</text>
  </g>
    </g>
    <g class="violation" data-layers="layer-VIA1 layer-M1">
    <line class="violation-edge" x1="277.5" y1="495" x2="277.5" y2="542.5" stroke="red" stroke-width="3" stroke-linecap="round" />
    <line class="violation-edge" x1="230" y1="400" x2="230" y2="637.5" stroke="red" stroke-width="3" stroke-linecap="round" />
  <g>
    <line class="violation-marker" x1="277.5" y1="495" x2="230" y2="495" stroke="red" stroke-width="2" stroke-linecap="round" />
    <circle class="violation-point" cx="277.5" cy="495" r="4" fill="red" />
    <circle class="violation-point" cx="230" cy="495" r="4" fill="red" />
    <text class="violation-label" x="253.75" y="488" font-size="14" text-anchor="middle" fill="red">Enclosure 1/2</text>
  </g>
    </g>
    <g class="violation" data-layers="">
  <g>
    <rect class="violation-region" x="515" y="162.5" width="475" height="475" fill="red" fill-opacity="0.08" stroke="red" stroke-width="2" />
    <text class="violation-label" x="752.5" y="180.5" font-size="14" text-anchor="middle" fill="red">MinDensity 0.4/0.5</text>
  </g>
    </g>
  </g>
  <g id="layer-legend">
    <text x="1050" y="40" font-size="20" font-weight="bold" fill="black">Violations: 5</text>
    <text x="1050" y="65" font-size="18" font-weight="bold" fill="black">Layers</text>
    <text id="toggle-all-layers" x="1115" y="65" font-size="13" fill="#555555" text-decoration="underline" style="cursor: pointer;" onclick="toggleAllLayers()">Hide All</text>
    <g class="legend-item" data-layer="layer-M1" onclick="toggleLayer(this)" style="cursor: pointer;">
    <rect x="1050" y="90" width="18" height="18" fill="cyan" fill-opacity="0.35" stroke="cyan" stroke-width="2" />
      <text class="legend-label" x="1078" y="105" font-size="14" fill="black">M1</text>
    </g>
    <g class="legend-item" data-layer="layer-VIA1" onclick="toggleLayer(this)" style="cursor: pointer;">
    <rect x="1050" y="120" width="18" height="18" fill="yellow" fill-opacity="0.35" stroke="yellow" stroke-width="2" />
      <text class="legend-label" x="1078" y="135" font-size="14" fill="black">VIA1</text>
    </g>
  </g>
  <script><![CDATA[
    function updateLayerVisibility() {
      const hiddenLayers = new Set();

      document.querySelectorAll('.legend-item.disabled').forEach(function(item) {
        hiddenLayers.add(item.dataset.layer);
      });

      document.querySelectorAll('.shape').forEach(function(shape) {
        let hideShape = false;

        shape.classList.forEach(function(className) {
          if (className.startsWith('layer-') && hiddenLayers.has(className)) {
            hideShape = true;
          }
        });

        shape.style.display = hideShape ? 'none' : '';
      });

      document.querySelectorAll('.violation').forEach(function(violation) {
        const layersText = violation.dataset.layers || '';
        const layers = layersText === '' ? [] : layersText.split(' ');

        let hideViolation = false;

        layers.forEach(function(layer) {
          if (hiddenLayers.has(layer)) {
            hideViolation = true;
          }
        });

        violation.style.display = hideViolation ? 'none' : '';
      });
    }

    function updateToggleAllText() {
      const legendItems = document.querySelectorAll('.legend-item');
      const toggleText = document.getElementById('toggle-all-layers');

      if (!toggleText || legendItems.length === 0) {
        return;
      }

      const allHidden = Array.from(legendItems).every(function(item) {
        return item.classList.contains('disabled');
      });

      toggleText.textContent = allHidden ? 'Show All' : 'Hide All';
    }

    function toggleAllLayers() {
      const legendItems = document.querySelectorAll('.legend-item');

      const allHidden = Array.from(legendItems).every(function(item) {
        return item.classList.contains('disabled');
      });

      legendItems.forEach(function(item) {
        if (allHidden) {
          item.classList.remove('disabled');
        }
        else {
          item.classList.add('disabled');
        }
      });

      updateLayerVisibility();
      updateToggleAllText();
    }

    function toggleLayer(legendItem) {
      legendItem.classList.toggle('disabled');
      updateLayerVisibility();
      updateToggleAllText();
    }

    const svg = document.documentElement;

    const originalWidth = 1200;
    const originalHeight = 800;

    let viewX = 0;
    let viewY = 0;
    let viewWidth = originalWidth;
    let viewHeight = originalHeight;

    const MIN_ZOOM_FACTOR = 0.1;

    svg.addEventListener('wheel', function(event) {
      event.preventDefault();

      const rect = svg.getBoundingClientRect();

      const mouseX = viewX +
        ((event.clientX - rect.left) / rect.width) * viewWidth;

      const mouseY = viewY +
        ((event.clientY - rect.top) / rect.height) * viewHeight;

      let zoomFactor = event.deltaY < 0 ? 0.9 : 1.1;

      let newWidth = viewWidth * zoomFactor;

      const minWidth = originalWidth * MIN_ZOOM_FACTOR;

      if (newWidth < minWidth) {
        newWidth = minWidth;
      }

      if (newWidth > originalWidth) {
        newWidth = originalWidth;
      }

      zoomFactor = newWidth / viewWidth;

      const newHeight = viewHeight * zoomFactor;

      viewX = mouseX - (mouseX - viewX) * zoomFactor;
      viewY = mouseY - (mouseY - viewY) * zoomFactor;

      viewWidth = newWidth;
      viewHeight = newHeight;

      if (viewWidth == originalWidth) {
        viewX = 0;
        viewY = 0;
        viewHeight = originalHeight;
      }

      svg.setAttribute(
        'viewBox',
        viewX + ' ' + viewY + ' ' + viewWidth + ' ' + viewHeight
      );
    }, { passive: false });

    function resetView() {
      viewX = 0;
      viewY = 0;
      viewWidth = originalWidth;
      viewHeight = originalHeight;

      svg.setAttribute(
        'viewBox',
        '0 0 ' + originalWidth + ' ' + originalHeight
      );
    }

    document.addEventListener('keydown', function(event) {
      if (event.key === 'f' || event.key === 'F') {
        resetView();
      }
    });
  ]]></script>
</svg>
